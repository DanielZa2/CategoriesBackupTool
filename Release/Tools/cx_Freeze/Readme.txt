This is a semi-succesful attempt to make an self-contained excecutable file from the python code using cx_freeze.
To create the exe using cx_freeze copy the python files here and run:
python setup.py build